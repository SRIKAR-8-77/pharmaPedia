# ── ML / NLP extras (install on top of requirements.txt) ─────────────────────
# Run: uv pip install -r requirements-ml.txt
#
# Removed from this file (already in requirements.txt):
#   spacy, presidio-analyzer, presidio-anonymizer, datasketch
#
# Removed scispacy + medspacy — they require spacy<3.8 which conflicts
# with spacy==3.8.14 already installed from requirements.txt.
# Use the spaCy en_core_sci_sm model directly instead.

# Text cleaning
ftfy==6.2.0
langdetect==1.0.9

# Transformer-based sentiment + embeddings
transformers==4.44.2
torch==2.4.1

# Semantic deduplication + similarity search
faiss-cpu==1.8.0
sentence-transformers==3.1.1

# Web scraping (Phase 5)
scrapy==2.11.2
playwright==1.47.0
fake-useragent==1.5.1
